# Agent Operating Notes

- After completing any change to this repository, immediately run `git status`, review the diff, and create a git commit with a meaningful message that describes the work.
- Keep edits scoped and descriptive so that each commit tells a clear story when someone else reviews the history.
